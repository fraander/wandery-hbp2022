import SwiftUI
import MapKit

struct ContentView: View {
    var body: some View {
        MainView()
//        PlaceListView(list: PlaceList.preview1)
//        PlaceDetailView(place: Place.preview1)
    }
}
